# Dependencias de platform/ — versiones PINNED (probadas en el Mini PC, 2026-08-11).
# Instalar en la máquina nueva:  python -m pip install -r requirements.txt
# Ver docs/MIGRACION-GPU.md para el runbook completo.
flask==3.1.3
instagrapi==2.18.12
uiautomator2>=2.16
requests[socks]==2.34.2
pysocks>=1.7
python-dotenv>=1.0
psutil>=5.9
# MCP server (Streamable HTTP) para agentes — FastMCP 1.x (2.x cambió la API)
mcp==1.29.0
uvicorn>=0.30
# taktik-bot (engagement): se instala desde el repo git (pin del commit probado)
taktik-bot @ git+https://github.com/masterFuf/taktik-bot.git@c2b748967622c26cc478aa48ebbd2493ab2d1086
